# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local config
.claude/

# Logs
*.log
npm-debug.log*

# Env / secrets (por si algún día agregás un .env)
.env
.env.local
.env.*.local

# Build artifacts (por si en el futuro hay build)
dist/
build/
node_modules/

# Netlify local
.netlify/

# Video sources (no van a producción — solo el hero.mp4 final)
*.MOV
*.mov
out.mp4
out*.mp4
copy_*.MOV
